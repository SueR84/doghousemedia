---
import Layout from "../layouts/Layout.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";

const team = [
  {
    name: "Emma Walkin",
    role: "Founder",
    bio: "Leads media production and oversees overall studio operations — from first brief to final grade.",
    photo: "/assets/team/emma-walkin.jpeg",
    linkedin: "https://www.linkedin.com/in/emma-walkin-57865041",
  },
  {
    name: "Susan Ryan",
    role: "Founder",
    bio: "Over 20 years of IT experience across Ireland and the UK — spanning programming, consultancy, and project management.",
    photo: "/assets/team/susan-ryan.jpeg",
    linkedin: "https://www.linkedin.com/in/susan-ryan-4b4b8627",
  },
];
---

<Layout title="About Us — Doghouse Media">
  <Nav />

  <main>
    <!-- Page header -->
    <section class="about-hero">
      <div class="wrap">
        <div class="eyebrow">About</div>
        <h1>A small Dublin team with a big passion for craft.</h1>
        <p class="about-lead">
          We've been helping good brands get noticed since 2013. Two disciplines
          under one roof — web and video — so nothing gets lost in translation.
        </p>
      </div>
    </section>

    <!-- How we work -->
    <section class="about-section reveal">
      <div class="wrap about-grid">
        <div>
          <span class="kicker">How we work</span>
          <h2>Built on trust and honest delivery.</h2>
          <p>
            We start every project with a proper conversation — getting to know
            your objectives, your audience, and what success looks like for you.
            From there, we keep you in the loop at every stage, so there are no
            surprises at the end.
          </p>
          <p>
            Whether it's a custom website you can manage yourself or a marketing
            video that stops the scroll, the goal is the same: work that makes
            you stand out and holds up long after launch.
          </p>
        </div>
        <div class="about-aside">
          <div class="about-stat">
            <span class="stat-num">2013</span>
            <span class="stat-label">Founded</span>
          </div>
          <div class="about-stat">
            <span class="stat-num">2</span>
            <span class="stat-label">Crafts, one roof</span>
          </div>
          <div class="about-stat">
            <span class="stat-num">Dublin</span>
            <span class="stat-label">Based in Ireland</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Team -->
    <section class="about-team reveal">
      <div class="wrap">
        <span class="kicker">The team</span>
        <div class="team-grid">
          {
            team.map((member) => (
              <div class="team-card">
                <img
                  class="team-avatar"
                  src={member.photo}
                  alt={member.name}
                  width="500"
                  height="500"
                  loading="lazy"
                />
                <h3>{member.name}</h3>
                <span class="team-role">{member.role}</span>
                <p>{member.bio}</p>
                <a
                  class="team-linkedin"
                  href={member.linkedin}
                  target="_blank"
                  rel="noopener"
                >
                  <svg viewBox="0 0 24 24" width="15" height="15" aria-hidden="true" fill="currentColor">
                    <path d="M4.98 3.5a2.5 2.5 0 1 1 0 5 2.5 2.5 0 0 1 0-5zM3 9h4v12H3zM9 9h3.8v1.64h.05c.53-1 1.83-2.05 3.77-2.05 4.03 0 4.78 2.65 4.78 6.1V21h-4v-5.4c0-1.29-.02-2.95-1.8-2.95-1.8 0-2.08 1.4-2.08 2.85V21H9z"/>
                  </svg>
                  LinkedIn
                </a>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="wrap cta-wrap">
      <div class="cta reveal">
        <div class="cta-inner">
          <h2>Like what you see? <span class="red">Let's talk.</span></h2>
          <p>Tell us your idea and we'll tell you straight whether we're the right studio for it.</p>
          <a href="mailto:susan@doghousemedia.ie" class="btn btn-cream">Get in touch</a>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</Layout>

<style>
  .about-hero {
    padding: 80px 0 64px;
  }
  .about-hero h1 {
    font-family: var(--disp);
    font-weight: 800;
    font-size: clamp(36px, 5vw, 64px);
    letter-spacing: -0.04em;
    line-height: 1.02;
    max-width: 16ch;
    margin: 12px 0 24px;
  }
  .about-lead {
    font-size: clamp(17px, 1.8vw, 20px);
    max-width: 52ch;
    opacity: 0.75;
    line-height: 1.55;
  }

  .about-section {
    padding: 64px 0;
    border-top: 1px solid var(--line);
  }
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 280px;
    gap: 80px;
    align-items: start;
  }
  .about-grid h2 {
    font-family: var(--disp);
    font-weight: 800;
    font-size: clamp(24px, 2.8vw, 34px);
    letter-spacing: -0.035em;
    line-height: 1.1;
    margin: 10px 0 20px;
  }
  .about-grid p {
    font-size: 16px;
    line-height: 1.65;
    opacity: 0.8;
    margin-bottom: 16px;
  }

  .about-aside {
    display: flex;
    flex-direction: column;
    gap: 28px;
    padding-top: 42px;
  }
  .about-stat {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
  .stat-num {
    font-family: var(--disp);
    font-weight: 800;
    font-size: 32px;
    letter-spacing: -0.03em;
    color: var(--ink);
  }
  .stat-label {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--gray);
  }

  .about-team {
    padding: 64px 0 80px;
    border-top: 1px solid var(--line);
  }
  .team-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 40px;
    margin-top: 32px;
    max-width: 720px;
  }
  .team-card {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .team-avatar {
    width: 96px;
    height: 96px;
    border-radius: 50%;
    background: var(--steel-tint);
    object-fit: cover;
    margin-bottom: 14px;
  }
  .team-linkedin {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    margin-top: 12px;
    font-family: var(--mono);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--teal);
    text-decoration: none;
    transition: color 0.15s;
  }
  .team-linkedin:hover {
    color: var(--red);
  }
  .team-card h3 {
    font-family: var(--disp);
    font-weight: 700;
    font-size: 18px;
    letter-spacing: -0.02em;
  }
  .team-role {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--teal);
    margin-bottom: 8px;
  }
  .team-card p {
    font-size: 17px;
    line-height: 1.65;
    opacity: 0.78;
  }

  @media (max-width: 760px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }
    .about-aside {
      flex-direction: row;
      flex-wrap: wrap;
      padding-top: 0;
    }
    .team-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  (function () {
    const els = document.querySelectorAll<HTMLElement>(".reveal");
    if (window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches)
      return;
    els.forEach((el) => el.classList.add("armed"));
    const io = new IntersectionObserver(
      (entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            e.target.classList.add("in");
            io.unobserve(e.target);
          }
        });
      },
      { threshold: 0.12, rootMargin: "0px 0px -6% 0px" }
    );
    els.forEach((el) => io.observe(el));
    setTimeout(() => {
      els.forEach((el) => {
        if (!el.classList.contains("in")) el.classList.add("in");
      });
    }, 1200);
  })();
</script>
